# Claude Instructions

Follow `AGENTS.md` as the primary repository instruction file.

When using Ariadne:

- Keep small tasks lightweight.
- Load skill files only when the task calls for them.
- For complex tasks, use task-router, goal framing, baseline-first, spec writing, sub-agent delegation when useful, verification, and memory capture.
- Do not claim completion without evidence.
