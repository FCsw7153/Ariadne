# Ariadne Claude Instructions

Follow `AGENTS.md` as the primary repository instruction file.

Claude-specific usage should stay lightweight:

- Treat this repository as a method pack, not a platform implementation.
- Load skill files only when the task calls for them.
- Keep discussions exploratory until the user asks to implement concrete files.
- For complex tasks, ask targeted questions before writing broad specs.
